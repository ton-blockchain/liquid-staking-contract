;; common functions in all contracts
global slice governor;
global int state;

() process_halt_request(slice sender, slice in_msg) impure inline_ref {
    assert_sender!(sender, governor);
    state = state::HALTED;
}
